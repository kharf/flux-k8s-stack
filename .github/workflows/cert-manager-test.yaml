# AUTOGENERATED - DO NOT EDIT

name: cert-manager-test
"on":
  workflow_dispatch:
    branches:
      - main
  push:
    branches:
      - main
    tags-ignore:
      - '*'
    paths:
      - catalog/cert-manager/**
  pull_request:
    branches:
      - '*'
    tags-ignore:
      - '*'
    paths:
      - catalog/cert-manager/**
env:
  GITHUB_USER: kharf
  GITHUB_TOKEN: ${{ secrets.PAT }}
  REPOSITORY_URL: https://github.com/kharf/flux-k8s-stack
  BRANCH: ${{ github.head_ref || github.ref_name }}
  CLUSTER_NAME: cert-manager
  KS_PATH: ./catalog/cert-manager/test
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.3
        with:
          token: ${{ secrets.PAT }}
      - name: Test
        run: ./scripts/local-setup.sh
permissions: read-all
